代码重构请求

我现在正在开发一个BBO项目，但是出现了很大的问题，我的项目内的很多代码被别的AI agent恶意篡改了，导致整个项目文件和跑脚本的运行逻辑出现了问题，问题如下：
1. 我的所有项目分区设计，代码的思路，项目分工，运行脚本设计的思路都是对的
2. 但是我现在因为agent的恶意攻击，我的代码间埋藏了很多冗余逻辑和错误分支，导致实际运行的时候那些正确的部分根本没办法运行出来
3. 并且我现在分区和分区之间的连接逻辑有一些非常不明显但是伤害很大的漏洞，导致一些参数传递错误，和一些实现错误，导致结果是代码跑不通

我希望你根据我以下的描述，先建立对这个项目的大致workflow的了解，再明确我跑实验的pipeline，再细致的理解我目前规划的每个文件的功能以及实现细节，最后根据这个规划自行设计修补这个项目的方案

下面我将开始描述：

首先是项目分区，我的设计分为
1. baseline区（存储算法在不同benchmark间的通用实现，只写BBO算法逻辑和统一化的优化接口，方便测试脚本调用）———其中包含bo，turbo，hesbo，saasbo，nevergrad，lamcts，baxus，cmaes这八个算法实现
2. 我的算法scalpel——这个scalpel是我自己设计的BBO算法，和baseline里面的算法接口不是对齐的，所以在我的scalpel分区内我会把我现有的scalpel算法给包装一下（wrap出合适的接口），使得其可以与其他算法一样可以被统一化的接口调用
3. functions区（不用改这个分区名），里面储存了我希望用以评测各个BBO- baseline算法的benchmark，分别是cec- functions（这里面包括ackley，griedwank，rosenbrock，rasterign，levy，等一共七个测试函数），mujoco环境（包含swimmer，hopper，ant，humanoid，half- cheetah，等一共六个）以及lasso-bench（用了sync-high，sync-hard，以及lasso-DNA这三个lasso测试任务）———这个分区的大致实现逻辑基本无误，就是包装接口以及设计优化任务（比如原本是reward正向优化会有加一个负号使得符合bbo- minimize的优化目标
4. 脚本区（这个分区没有单独的文件夹而是一些脚本，针对每个benchmark都写了run_bbo_｛benchmark_name｝.py的脚本（不同脚本内有不同的命令行参数），每个脚本都有对应的保存逻辑，会把运行得到的结果以json形式保存，并且会有日志记录并且我还有一个run_bbo.sh是一个命令行脚本，可以自由选择benchmark类型（以及每个benchmark内的小类）和算法内容———这块是逻辑重叠的重灾区
5. utils区（这个分区包括一些实用的小脚本——可视化，gpu分配，环境包装等），你可以阅读这个分区内的代码确保其功能是正确的！——尤其是GPU分配，我希望可以并行的运行

其次是我遇到的问题：
1. 项目分区管理切割的不干净——比如reward要取负号再优化最后结果保存时还要再取回负号这样才是完整的优化过程，理论上这个逻辑应该是，在functions分区下的mujoco的benchmark内部把这个处理一下，在传给optimizer的时候就是已经处理好的，并且在别处不需要再处理，然后直到保存在json的前一步通过识别脚本（run_bbo_mujoco.py这个脚本）来决定要不要再负号回来保存正确的reward增长结果！————但是实际上现在这个功能就写的乱七八糟，在baseline分区里面也有纠缠这个逻辑，非常恶心，很容易报错并且误导
2. 我只需要评测bo，hesbo，turbo，scalpel，baxus，cmaes，saasbo这七个算法———这些算法本身的实现很可能是有问题的（比如理论上bo肯定是效果最差的，baxus这种SOTA的算法应该效果很好，我的scalpel是自己科研出来的已经被我的collaborator证明是SOTA的，但是会出现在运行一些benchmark的时候baxus和scalpel都不如bo的情况，这非常的匪夷所思！！！）———所谓问题主要集中在三个方面：1. 算法逻辑谬误，即不因为思想/设计而是因为代码逻辑出现低级失误导致算法失效；2. 算法参数设置不恰当（没有根据问题规模改变）或者错误退化为random/其他算法（这些是其他agent为了跑通代码做的一些影响正确性的不恰当修改）；3. 算法本身实现有误，比如并非真正正确的实现，或者是调包方式错误，等等）————你需要检查这些问题并且修改
3. gpu管理出大问题——现在所有baseline算法文件和测试脚本的gpu管理都是自己写自己的，也许可行但是乱的要命———我实际上希望gpu-id只由utils分区内的scheduler统一调度，即run_bbo.sh这个脚本内无需传入gpu参数，整个这个run_bbo_benchmark这种测试脚本内有一个统一工厂函数，这个工厂函数不会显式分配gpu号，而是等他对应到某一个算法的时候，由baseline分区内的算法文件调用utils的gpu-scheduler来为每个算法分配gpu（反正最后的优化器调用都是调用baseline里面的算法优化器类的实现！———而我的机器上正好有七个GpU对应我七个需要评测的baseline算法）
4. 后台进程管理有误：我在run_bbo.sh内用nohup把任务按照每个算法并行，但是每个算法的子任务都是串行的——这个逻辑目前在.sh脚本里面是正确的，但是脚本run_bbo_benchmark.py内的调用可能因为工厂函数把bbo过程托管给baseline里面的optimizer而导致进程管理错误，本来assign给cuda：5的任务在cuda：0甚至是非gpu进程上被启动——这个你需要特别小心并且不应该出这种问题
5. 保存逻辑混乱——我的保存是按照｛benchmark类｝/result/ ｛子benchmark名称｝/｛x维度｝/｛算法名称｝/这样的目录保存的，并且保存内容有：有run_bbo_benchmark这类脚本生成的两个.json，一个是progress.json，用于记录优化过程（这个过程的步长较小，一般设计为整个budget的1/1000，比如2000budget那就是2步一记录），一个是final_result.json，这个用于记录最终结果以及对应的x值（x对应dim不同），还有需要上传的wandb数据——这个上传的逻辑需要你斟酌，我个人认为应该是在保存json的前一刻把你保存的数据同步上传wandb，并且这个wandb设置应该在每个小的优化子任务开始的时候完成、需要把wandb组织成一个大project名为bbo，七个小算法整出来七个group分别命名，然后小的group下才有每次跑实验的名称，以及对应的实验时间（取真实时间、这样可以区分每次运行）

这些问题都需要被依次解决！请你阅读完毕之后，有规划的调整每个问题并且检查逻辑
